# Verification report

Generated by `bench/verify.py` from self-generated targets (no private data).
Each target is built into a Ghidra project, then the same canonical dump is
extracted via the **bridge** and via **pyghidra**, and compared byte-for-byte.

**Verdict: OK — identical to pyghidra everywhere**

| Target | Result | Notes |
| --- | --- | --- |
| jvm | 8/8 identical |  |
| x86-64 | 8/8 identical |  |
| aarch64 | 8/8 identical |  |
| arm | 8/8 identical |  |
| riscv64 | 8/8 identical |  |
| x86-32 | 8/8 identical |  |

\* decompile output matched everywhere except where Ghidra's own decompiler is nondeterministic (confirmed by running pyghidra twice) — not a bridge difference.